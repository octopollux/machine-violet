// Dev overrides — currently inert. JSON.parse fails on these comments and the
// loader (packages/engine/src/config/models.ts) silently falls back to the
// hardcoded DEFAULTS. Per-tier model selection now lives in connections.json
// (managed by the Connections UI), so dev-config no longer needs to override
// model IDs. The effort values below mirror the current production defaults
// in models.ts — kept here as documentation in case you ever want to flip
// one for local experimentation (delete this comment block to re-enable).
//
// {
//   "effort": {
//     "default": null,
//     "dm": "high",
//     "ooc": "high",
//     "setup": "high",
//     "dev-mode": "high",
//     "ai-player": "low",
//     "promote_character": "medium",
//     "repair-state": "medium"
//   }
// }
